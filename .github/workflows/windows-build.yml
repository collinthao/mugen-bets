name: Build and package Mugen-Bets

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

# Allow workflow to create/update releases with GITHUB_TOKEN
permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          lfs: true
          fetch-depth: 0

      - name: Ensure Git LFS objects
        run: |
          git lfs install --local || true
          git lfs pull || true

      - name: Setup NuGet
        uses: NuGet/setup-nuget@v1

      - name: Restore NuGet packages
        run: nuget restore Mugen-Bets.slnx

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v1

      - name: Build solution (locate MSBuild and run)
        shell: pwsh
        run: |
          $msbuild = $null
          $msbuildCmd = Get-Command msbuild -ErrorAction SilentlyContinue
          if ($msbuildCmd) { $msbuild = $msbuildCmd.Source }
          if (-not $msbuild) {
            $vswhere = "$env:ProgramFiles(x86)\Microsoft Visual Studio\Installer\vswhere.exe"
            if (Test-Path $vswhere) {
              $inst = & $vswhere -latest -products * -requires Microsoft.Component.MSBuild -property installationPath
              if ($inst) {
                $candidate = Join-Path $inst "MSBuild\Current\Bin\MSBuild.exe"
                if (Test-Path $candidate) { $msbuild = $candidate }
              }
            }
          }
          if (-not $msbuild -or -not (Test-Path $msbuild)) { Write-Error "MSBuild not found on runner"; exit 1 }
          & $msbuild "Mugen-Bets.slnx" /m /p:Configuration=Release /p:Platform=x64

      - name: Locate exe and prepare artifact
        shell: pwsh
        run: |
          $outDir = "$env:GITHUB_WORKSPACE\build_artifact"
          New-Item -ItemType Directory -Path $outDir -Force | Out-Null
          $exe = Get-ChildItem -Path $env:GITHUB_WORKSPACE -Filter *.exe -Recurse | Where-Object { $_.Name -match 'Mugen-Bets.exe' } | Select-Object -First 1
          if (-not $exe) { Write-Error "Executable not found"; exit 1 }
          Write-Host "Copying exe: $($exe.FullName) -> $outDir"
          Copy-Item -Path $exe.FullName -Destination $outDir -Force

          # Include VC++ 2008 SxS CRT from repo if present, otherwise download and extract the redistributable in CI
          $vc90src = Join-Path $env:GITHUB_WORKSPACE 'vcredist\Microsoft.VC90.CRT'
          $vc90dest = Join-Path $outDir 'Microsoft.VC90.CRT'
          if (Test-Path $vc90src) {
            Write-Host "Including Microsoft.VC90.CRT from repo: $vc90src -> $vc90dest"
            Copy-Item -Path $vc90src -Destination $vc90dest -Recurse -Force
          }
          else {
            Write-Host "Microsoft.VC90.CRT not found in repo; attempting to download and extract vcredist_x86.exe"
            $tmp = Join-Path $env:TEMP "vcredist_extract"
            $vcredistExe = Join-Path $env:TEMP "vcredist_x86.exe"
            if (-not (Test-Path $vcredistExe)) {
              $url = 'https://download.microsoft.com/download/1/5/8/158E5E2F-5A2E-4E6C-A9C7-7E63E0B0E5E8/vcredist_x86.exe'
              Write-Host "Downloading vcredist from $url"
              Invoke-WebRequest -Uri $url -OutFile $vcredistExe -UseBasicParsing -ErrorAction Stop
            }
            Remove-Item -Recurse -Force -ErrorAction SilentlyContinue $tmp
            New-Item -ItemType Directory -Path $tmp -Force | Out-Null
            Write-Host "Extracting $vcredistExe to $tmp"
            # Use the redistributable's self-extract to get the redist\Microsoft.VC90.CRT folder
            $proc = Start-Process -FilePath $vcredistExe -ArgumentList "/x:$tmp","/Q" -NoNewWindow -Wait -PassThru
            if ($proc.ExitCode -ne 0) { Write-Host "Extraction exit code: $($proc.ExitCode); attempting without /Q"; Start-Process -FilePath $vcredistExe -ArgumentList "/x:$tmp" -NoNewWindow -Wait }
            $extracted = Join-Path $tmp 'redist\Microsoft.VC90.CRT'
            if (Test-Path $extracted) {
              Write-Host "Copying extracted Microsoft.VC90.CRT -> $vc90dest"
              Copy-Item -Path $extracted -Destination $vc90dest -Recurse -Force
            }
            else {
              Write-Host "Extraction did not produce expected folder $extracted. Skipping CRT bundling."
            }
          }

      - name: Zip artifact
        shell: pwsh
        run: |
          $zip = "$env:GITHUB_WORKSPACE\Mugen-Bets-release.zip"
          if (Test-Path $zip) { Remove-Item $zip }
          Compress-Archive -Path "$env:GITHUB_WORKSPACE\build_artifact\*" -DestinationPath $zip -Force

      - name: Verify ZIP contents include font and chars
        shell: pwsh
        run: |
          $zip = "$env:GITHUB_WORKSPACE\Mugen-Bets-release.zip"
          if (-not (Test-Path $zip)) { Write-Error "ZIP not found: $zip"; exit 1 }
          Add-Type -AssemblyName System.IO.Compression.FileSystem
          $zf = [System.IO.Compression.ZipFile]::OpenRead($zip)
          $entries = $zf.Entries | ForEach-Object { $_.FullName }
          $zf.Dispose()
          # Ensure the release contains the built executable only
          $required = @('Mugen-Bets.exe')
          $missing = @()
          foreach ($r in $required) {
            if (-not ($entries -contains $r)) { $missing += $r }
          }
          if ($missing.Count -gt 0) { Write-Error "Missing required entries in ZIP: $missing"; exit 1 }
          Write-Host "ZIP verification passed"

      - name: Create-or-find public Release and upload asset (REST)
        shell: pwsh
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          $token = $env:GITHUB_TOKEN
          $repoFull = $env:GITHUB_REPOSITORY
          $parts = $repoFull -split '/' 
          $owner = $parts[0]
          $repo = $parts[1]
          $tag = 'Release'
          $apiBase = "https://api.github.com/repos/$owner/$repo"
          $headers = @{ Authorization = "token $token"; Accept = 'application/vnd.github.v3+json' }

          Write-Host "Checking for existing release $tag"
          try {
            $release = Invoke-RestMethod -Uri "$apiBase/releases/tags/$tag" -Headers $headers -Method Get -ErrorAction Stop
            Write-Host "Found existing release id=$($release.id)"
            $releaseId = $release.id
          }
          catch {
            Write-Host "Release not found; creating"
            $body = @{ tag_name = $tag; name = $tag; body = "Automated CI build"; draft = $false; prerelease = $false } | ConvertTo-Json
            $release = Invoke-RestMethod -Uri "$apiBase/releases" -Headers $headers -Method Post -Body $body -ContentType 'application/json' -ErrorAction Stop
            $releaseId = $release.id
            Write-Host "Created release id=$releaseId"
          }

          $assetPath = "$env:GITHUB_WORKSPACE\Mugen-Bets-release.zip"
          if (-not (Test-Path $assetPath)) { Write-Error "Asset not found: $assetPath"; exit 1 }
          $fileName = Split-Path $assetPath -Leaf
          $escapedName = [System.Uri]::EscapeDataString($fileName)
          $uploadUri = "https://uploads.github.com/repos/$owner/$repo/releases/$releaseId/assets?name=$escapedName"
          Write-Host "uploadUri: $uploadUri"
          $headers = @{ Authorization = "token $token"; Accept = 'application/vnd.github.v3+json' }
          # Check for existing asset with same name and delete it to allow re-upload
          try {
            $assets = Invoke-RestMethod -Uri "$apiBase/releases/$releaseId/assets" -Headers $headers -Method Get -ErrorAction Stop
            $existing = $assets | Where-Object { $_.name -eq $fileName } | Select-Object -First 1
            if ($existing) {
              Write-Host "Deleting existing asset id=$($existing.id) name=$($existing.name)"
              Invoke-RestMethod -Uri "https://api.github.com/repos/$owner/$repo/releases/assets/$($existing.id)" -Headers $headers -Method Delete -ErrorAction Stop
            }
          }
          catch {
            Write-Host "No existing assets or failed to list assets: $_"
          }

          # Use curl.exe to upload the asset to avoid transient .NET transport errors
          $curlExe = 'curl.exe'
          if (-not (Get-Command $curlExe -ErrorAction SilentlyContinue)) { Write-Error "curl.exe not found on runner"; exit 1 }
          $curlArgs = @('-sS','-X','POST','-H',"Authorization: token $token",'-H','Content-Type: application/zip','--data-binary',"@$assetPath",$uploadUri)
          Write-Host "Uploading with curl.exe: $curlExe $($curlArgs -join ' ')"
          $proc = Start-Process -FilePath $curlExe -ArgumentList $curlArgs -NoNewWindow -Wait -PassThru
          if ($proc.ExitCode -ne 0) { Write-Error "curl upload failed with exit code $($proc.ExitCode)"; exit $proc.ExitCode }
          Write-Host "Upload complete"

      - name: Create GitHub Release (on tag)
        if: startsWith(github.ref, 'refs/tags/')
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref_name }}
          release_name: Release ${{ github.ref_name }}
          draft: false
          prerelease: false
          body: "Automated release for ${{ github.ref_name }}"

      - name: Upload release asset (on tag)
        if: startsWith(github.ref, 'refs/tags/')
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: Mugen-Bets-release.zip
          asset_name: Mugen-Bets-release-${{ github.ref_name }}.zip
          asset_content_type: application/zip

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: Mugen-Bets-release
          path: Mugen-Bets-release.zip
